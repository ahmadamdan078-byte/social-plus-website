[build]
builder = "NIXPACKS"
buildCommand = "npm run build && npm install --prefix server"

[deploy]
startCommand = "NODE_PATH=./server/node_modules node server.js"
healthcheckPath = "/api/public/health"
restartPolicyType = "ON_FAILURE"
