/**
 * Admin configuration — add admin emails here
 */
window.SP_ADMIN_CONFIG = {
  emails: ['hamdanmustafa840@gmail.com'],
  /** Override URL for shared site settings (optional JSON file on your host) */
  overridesUrl: 'data/site-overrides.json'
};
